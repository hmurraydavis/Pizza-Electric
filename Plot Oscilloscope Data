%To use: Open>navigate to the workspace you have the data in>double click on the workspace name.
%Check in the command window to make sure the data you inported is a collumn matrix named "ans."
%Run this script.
%Type "clear all" into the command window before inporting/opening the next data set/work space.

plot(ans(:,1),ans(:,2),'LineWidth',1.3)
xlabel('Time','FontSize',16);
ylabel('Voltage (MilliVolts)','FontSize',16);
title('Electrical Response of Rochelle Salt Crystal','FontSize',20);
